import { SxProps } from "@mui/material"

type Props = {
    container: SxProps
}

export const layoutStyle: Props = {
    container: {
        px: { xs: 2, md: 4 },
        py: { xs: 2, md: 4 },
    }
}